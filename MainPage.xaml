<?xml version="1.0" encoding="utf-8" ?>
<ContentPage
    x:Class="firmware_upgrade.MainPage"
    xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
    xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
    x:Name="PageRef">

    <ScrollView>
        <VerticalStackLayout Padding="30,0" Spacing="25">
            <Image
                Aspect="AspectFit"
                HeightRequest="185"
                SemanticProperties.Description="dot net bot in a hovercraft number nine"
                Source="dotnet_bot.png" />
           

            <Label
                SemanticProperties.HeadingLevel="Level1"
                Style="{StaticResource Headline}"
                Text="Hello, World!" />

            <Label
                SemanticProperties.Description="Welcome to dot net Multi platform App U I"
                SemanticProperties.HeadingLevel="Level2"
                Style="{StaticResource SubHeadline}"
                Text="Welcome to &#10;.NET Multi-platform App UI" />

            <Label Text="Upgrade Progress:"
                   FontAttributes="Bold" />
            <ProgressBar Progress="{Binding UpgradeProgress, Converter={StaticResource PercentToDoubleConverter}}" />
            <Label Text="{Binding UpgradeProgress, StringFormat='Progress: {0}%'}" />

            <Label
                Margin="0,20,0,0"
                FontAttributes="Bold"
                Text="Nearby Bluetooth Devices:" />
            <CollectionView
                x:Name="DevicesListView"
                Margin="0,0,0,20"
                ItemsSource="{Binding Devices}">
                <CollectionView.ItemTemplate>
                    <DataTemplate>
                        <StackLayout Margin="10">
                            <Label Text="{Binding Name}" />
                            <Button
                                Command="{Binding BindingContext.ConnectCommand, Source={x:Reference PageRef}}"
                                CommandParameter="{Binding .}"
                                Text="Connect" />
                            
                            
                        </StackLayout>
                    </DataTemplate>
                </CollectionView.ItemTemplate>
            </CollectionView>
        </VerticalStackLayout>
    </ScrollView>

</ContentPage>
